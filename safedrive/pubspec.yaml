name: safedrive
description: Car Insurance Quote App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.0
  http: ^1.2.0
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true